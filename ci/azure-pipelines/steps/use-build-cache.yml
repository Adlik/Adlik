parameters:
    key: ''
    path: ''
steps:
  - task: Cache@2
    displayName: Set up build cache
    inputs:
        key: v2 | ${{ parameters.key }} | $(Agent.OS) | $(Build.SourceVersion)
        path: ${{ parameters.path }}
        restoreKeys: |
            v2 | ${{ parameters.key }} | $(Agent.OS) | $(Build.SourceVersion)
            v2 | ${{ parameters.key }} | $(Agent.OS)
