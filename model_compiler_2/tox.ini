[testenv]
commands = {envpython} setup.py build_py
           bandit -r src tests
           flake8 src tests
           mypy --no-incremental -p model_compiler -p tests.model_compiler
           pylint src tests
           pytest --cov -n auto

deps = .[test]
